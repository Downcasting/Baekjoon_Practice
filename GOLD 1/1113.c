#include <stdio.h>

int main(void){

    /* 이건 오른쪽 아래 대각선으로 죽 내려가고,
    다시 오른쪽 아래부터 왼쪽 위로 죽 올라온느  방식으로 구현하기
    \내려갈 때는 왼쪽 위, 왼쪽 2개만 고려하고, 올라올 때는 오른쪽, 아래만 고려하면 될듯
    50*50 크기면 적절한 것이라고 생각됨(2초)*/

    return 0;
}